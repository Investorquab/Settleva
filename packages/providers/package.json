{
  "name": "@settleva/providers",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit --rootDir ../..",
    "test": "tsc",
    "build": "tsc"
  },
  "dependencies": {
    "@settleva/conditions": "workspace:*",
    "@settleva/verification": "workspace:*"
  }
}
