{
  "name": "settleva",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.15.0",
  "engines": {"node": ">=22"},
  "scripts": {"check": "pnpm -r check", "test": "pnpm -r test", "build": "pnpm -r build"}
}
